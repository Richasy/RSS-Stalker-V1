<Page
    x:Class="RSS_Stalker.Pages.FeedDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RSS_Stalker.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="{ThemeResource LineColor}" BorderThickness="0,0,0,1" Padding="0,10">
            <TextBlock Name="TitleTextBlock" FontFamily="{StaticResource Title}" TextAlignment="Center" TextWrapping="Wrap" FontSize="20" HorizontalAlignment="Center"/>
        </Border>
        <WebView Name="DetailWebView"
                 Grid.Row="1"
                 Loaded="DetailWebView_Loaded"
                 DefaultBackgroundColor="{ThemeResource PageColor}"
                 />
        <ProgressRing Name="LoadingRing" Style="{StaticResource BasicProgressRingStyle}" Grid.Row="1"/>
    </Grid>
</Page>
